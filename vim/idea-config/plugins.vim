" Adds a for argument in f.x. dia
set argtextobj

" adds f for argument in f.x. dif
set functiontextobj

" classic tpope commentary
set commentary

" highlight yanked selection
set highlightedyank

" adds i for argument in f.x. dai
set textobj-indent

" adds q (quotes) and b (braces) for argument in f.x. dab, diq
set textobj-indent

" adds surrounding motions
set surround

" File Explorer
set NERDTree
map <leader>ee :NERDTree<CR>
map <leader>ec :NERDTreeClose<CR>
